// Defaults for every stored setting. The popup and the content script
// both read from here.
const DEFAULT_SETTINGS = {
  enabled: true,
  targetCurrency: 'EUR',
  sourceCurrency: 'auto',
  replacePrice: false,
  badgeColor: '#67c1f5'
};
